<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="karelle.env.royal">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoroyalpizza"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".PizzaActivity" />
        <activity android:name=".ToppingsActivity" />
        <activity android:name=".DrinksActivity" />
        <activity android:name=".DesertActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".ZivaActivity" />
        <activity android:name=".SaladActivity" />
        <activity android:name=".ToppingsZivaActivity" />
        <activity android:name=".PastaActivity" />
        <activity android:name=".TypePastaActivity" />
        <activity android:name=".SaucePastaActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CheckOutActivity" />
        <activity android:name=".FinalActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="karelle.env.royal.WelcomeActivity" />
        </activity>
        <activity android:name=".TrackerActivity"></activity>
    </application>

</manifest>